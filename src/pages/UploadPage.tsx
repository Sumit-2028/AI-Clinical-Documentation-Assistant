import { useEffect, useMemo, useState, type ChangeEvent, type DragEvent } from 'react'
import { useMutation, useQueryClient } from '@tanstack/react-query'
import { uploadHandwrittenDocument, uploadMultilingualInput, uploadTypedDocument } from '../api'
import type { InputModality, ProcessingStatus } from '../contracts/common'
import type { Step1Output } from '../contracts/step1Output'
import { useStep1Output } from '../hooks/useStep1'
import { AlertIcon, ArrowIcon, CheckIcon, FileIcon, UploadIcon } from '../components/icons'
import { StatusBadge } from '../components/Badges'
import { SectionCard } from '../components/SectionCard'

const steps = ['Upload', 'Extraction', 'Confidence check', 'Verification', 'Ready for clinical analysis']
export function UploadPage() {
  const { data: output } = useStep1Output(); const [modality, setModality] = useState<InputModality>('multilingual'); const [fileName, setFileName] = useState(output?.source_document ?? ''); const [patientId, setPatientId] = useState(output?.patient_id ?? 'pat_00123'); const [encounterId, setEncounterId] = useState(output?.encounter_id ?? 'enc_2026_0817_01'); const [sourceLanguage, setSourceLanguage] = useState(output?.source_language ?? 'hi'); const [isDragging, setIsDragging] = useState(false); const queryClient = useQueryClient()
  const upload = useMutation({ mutationFn: async () => { const request = { patient_id: patientId, encounter_id: encounterId, modality, source_language: modality === 'multilingual' ? sourceLanguage : 'en' }; return modality === 'typed' ? uploadTypedDocument(request) : modality === 'handwritten' ? uploadHandwrittenDocument(request) : uploadMultilingualInput(request) }, onSuccess: () => { void queryClient.invalidateQueries({ queryKey: ['step1-output'] }) } })
  const currentStatus: ProcessingStatus = output?.processing_status ?? 'pending_human_verification'; const activeStep = currentStatus === 'failed' ? 2 : currentStatus === 'complete' ? 5 : 4; useEffect(() => { if (output && !fileName) setFileName(output.source_document) }, [output, fileName])
  const handleFile = (file: File | undefined) => { if (file) setFileName(file.name) }; const handleDrop = (event: DragEvent<HTMLDivElement>) => { event.preventDefault(); setIsDragging(false); handleFile(event.dataTransfer.files[0]) }; const handleBrowse = (event: ChangeEvent<HTMLInputElement>) => handleFile(event.target.files?.[0])
  return <div className="page-stack"><div className="page-heading"><div><p className="eyebrow">INPUT PROCESSING</p><h1>Upload & process</h1><p className="page-subtitle">Bring clinical information into the patient record with a confidence check.</p></div><div className="heading-meta"><span className="live-dot" /> Processing system ready</div></div><div className="pipeline-card">{steps.map((step, index) => <div className={`pipeline-step ${index + 1 === activeStep ? 'current' : ''} ${index + 1 < activeStep ? 'done' : ''}`} key={step}><div className="pipeline-icon">{index + 1 < activeStep ? <CheckIcon /> : <span>{index + 1}</span>}</div><span>{step}</span>{index < steps.length - 1 && <div className="pipeline-line" />}</div>)}</div><div className="upload-grid"><SectionCard title="Source document" eyebrow="01 / INPUT" className="source-card"><div className={`dropzone ${isDragging ? 'dragging' : ''}`} onDragEnter={() => setIsDragging(true)} onDragOver={(event) => event.preventDefault()} onDragLeave={() => setIsDragging(false)} onDrop={handleDrop}><div className="upload-symbol"><UploadIcon /></div><h3>{fileName ? 'Document ready to process' : 'Drop a clinical document here'}</h3><p>PDF, PNG or JPG · up to 25 MB</p><label className="secondary-button browse-button">Browse files<input type="file" accept=".pdf,.png,.jpg,.jpeg" onChange={handleBrowse} /></label></div>{fileName && <div className="file-preview"><div className="file-icon"><FileIcon /></div><div><strong>{fileName}</strong><span>{modality === 'multilingual' ? 'Multilingual document' : `${modality[0].toUpperCase()}${modality.slice(1)} document`}</span></div><span className="file-ready"><CheckIcon /> Ready</span></div>}<div className="form-divider" /><div className="form-grid"><label>Patient search / select<div className="input-with-icon"><span className="patient-initials">AM</span><input value="Ananya Mehta" readOnly /><span className="select-caret">⌄</span></div></label><label>Patient ID<input value={patientId} onChange={(event) => setPatientId(event.target.value)} /></label><label>Encounter ID<input value={encounterId} onChange={(event) => setEncounterId(event.target.value)} /></label></div></SectionCard><SectionCard title="Document type" eyebrow="02 / ROUTING" className="modality-card"><p className="card-intro">Choose the document type so MedFlow can apply the right review safeguards.</p><div className="modality-options">{(['typed', 'handwritten', 'multilingual'] as InputModality[]).map((item) => <button key={item} className={`modality-option ${modality === item ? 'selected' : ''}`} onClick={() => setModality(item)}><span className="radio-indicator" /><span><strong>{item[0].toUpperCase() + item.slice(1)}</strong><small>{item === 'typed' ? 'Standard text document' : item === 'handwritten' ? 'Handwritten document with additional review' : 'Document in another language with translation'}</small></span></button>)}</div>{modality === 'multilingual' && <div className="language-panel"><div><label>Source language</label><select value={sourceLanguage} onChange={(event) => setSourceLanguage(event.target.value)}><option value="hi">Hindi</option><option value="ta">Tamil</option><option value="en">English</option></select></div><div><label>Translation confidence</label><div className="read-only-value">{output ? `${Math.round((output.translation_confidence ?? 0) * 100)}%` : '—'}</div></div><div className="original-text"><span>Original language text is preserved</span><p>{output?.original_language_text ?? 'Original text will appear here after extraction.'}</p></div></div>}<div className="route-note"><span className="route-note-icon"><CheckIcon /></span><span><strong>Processing method selected</strong><small>{modality === 'typed' ? 'Typed documents use standard text processing.' : 'This document will receive additional confidence review.'}</small></span></div><button className="primary-button process-button" onClick={() => upload.mutate()} disabled={upload.isPending || !fileName}>{upload.isPending ? 'Starting processing…' : 'Start processing'}<ArrowIcon /></button>{upload.isError && <p className="error-copy"><AlertIcon /> Unable to start processing. Try again.</p>}</SectionCard></div>{output && <ExtractionSnapshot output={output} />}</div>
}
function ExtractionSnapshot({ output }: { output: Step1Output }) { const reviewCount = useMemo(() => output.extracted_fields.filter((field) => field.requires_doctor_review_before_memory_write).length, [output]); return <SectionCard title="Latest processing result" eyebrow="03 / PROCESSING STATUS" action={<StatusBadge status={output.processing_status} />}><div className="job-summary"><div><span>Document</span><strong>{output.source_document}</strong></div><div><span>Processing record</span><strong>{output.job_id}</strong></div><div><span>Information extracted</span><strong>{output.extracted_fields.length}</strong></div><div><span>Needs review</span><strong className="review-number">{reviewCount}</strong></div></div></SectionCard> }
