import type { ClinicalEvent } from '../contracts/clinicalEvent'
import { CheckIcon, FileIcon, XIcon } from './icons'

export function ProvenanceDrawer({ event, onClose }: { event: ClinicalEvent; onClose: () => void }) {
  return <div className="drawer-backdrop" role="presentation" onClick={onClose}><aside className="provenance-drawer" role="dialog" aria-label="Source information" onClick={(click) => click.stopPropagation()}><div className="drawer-header"><div><p className="eyebrow">SOURCE INFORMATION</p><h2>Clinical finding details</h2></div><button className="icon-button" aria-label="Close source information" onClick={onClose}><XIcon /></button></div><div className="drawer-event"><span className="entity-pill">{event.entity_type}</span><strong>{event.normalized_concept}</strong><span>{event.event_local_id}</span></div><div className="drawer-section"><p className="drawer-label">Source document</p><div className="drawer-value"><FileIcon /><span>{event.source_document_id}</span></div></div><div className="drawer-section"><p className="drawer-label">Source text</p><div className="span-box"><div><span>Start</span><strong>{event.source_text_span.start}</strong></div><div><span>End</span><strong>{event.source_text_span.end}</strong></div><div><span>Length</span><strong>{event.source_text_span.end - event.source_text_span.start}</strong></div></div></div><div className="drawer-section"><p className="drawer-label">Source details</p><div className="metadata-list"><div><span>Input type</span><strong>{event.input_modality}</strong></div><div><span>Language</span><strong>{event.source_language.toUpperCase()}</strong></div><div><span>Translation confidence</span><strong className="verified-text">{event.translation_confidence === null ? 'Not applicable' : `${Math.round(event.translation_confidence * 100)}%`}</strong></div></div></div><div className="drawer-original"><p className="drawer-label">Original text</p><p>{event.original_text}</p></div><div className="drawer-footer"><CheckIcon /> Source information preserved for review</div></aside></div>
}
